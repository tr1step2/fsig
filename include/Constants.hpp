#pragma once

namespace fsig
{

namespace constants
{
    extern const size_t defaultBlockSize;
    extern const size_t defaultReadBufferSize;
    extern const size_t defaultWriteInterval;
}

}
