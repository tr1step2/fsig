#include "Constants.hpp"

namespace fsig
{

namespace constants
{
    const size_t mb = 1024 * 1024;

    const size_t defaultBlockSize = 1 * mb;
    const size_t defaultReadBufferSize = 10 * mb;
    const size_t defaultWriteInterval = 150;
}

}
